import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
    root: {
      flexGrow: 1,
    },
    logOutIcon: {
      float: "right",
      color: "#FFFFFF"
    },
    title: {
      flexGrow: 1,
      color: "#FFFFFF"
    },
    appBar: {
      height: "64px"
    }
  }));